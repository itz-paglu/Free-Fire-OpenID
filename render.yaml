services:
  - type: web
    name: free-fire-openid
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python -m camoufox fetch
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
